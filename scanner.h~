#ifndef SCANNER_H
#define SCANNER_H

#include <string>
#include <fstream>
#include <iostream>
#include <cstdlib>
#include "token.h"
#include <vector>
using namespace std;

class Scanner
{

	private:
	ifstream srcFile;
	Token token, nextTok;
	int lineNo;
	int colNo;
	vector<string> wordSymbols;	
	//static const string wordSymbols[17];// = {"begin", "end", "const", "array", "integer", "Boolean", "proc", "skip", "read", "write", "call", "if", "do","fi","od", "false","true"};
	string value;
	public:
	Scanner(Token tok);
	void start();
	Token nextToken();
	
		
};

#endif
