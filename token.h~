#ifndef TOKEN_H
#define TOKEN_H

#include <string>
#include "symbol.h" 

using namespace std;

class Token
{

	private:
	Symbol sym;
	string value;
	
	
	public:
	
	void setSymbol(Symbol symb);
	Symbol getSymbol();
	
};

#endif
